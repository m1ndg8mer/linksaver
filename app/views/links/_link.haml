%tr.info
  %td.text-center
    = link_to truncate(link.href, length: 30), link.href, target: '_blank'
  %td.text-center
    = link.description.gsub(/\n/, '<br>').html_safe
  %td.text-center
    -# defined at links_helper.rb
    = get_formatted_tags(link)
  %td.text-center
    = link.created_at.localtime.strftime("%B %d, %Y at %r")
  %td.text-center
    = link_to 'Edit', edit_link_path(link), class: 'btn btn-warning'
  %td.text-center
    -# defined at links_helper.rb
    = perform_link(link)
  %td.text-center
    = link_to 'Delete', link, method: :delete, data: {confirm: 'You sure?'}, class: 'btn btn-danger'