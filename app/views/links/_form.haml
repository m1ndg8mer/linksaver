= simple_form_for(@link, html: { class: 'form-horizontal' }) do |f|
  = f.input :href
  = f.input :description, as: :text, input_html: { rows: 10 }
  = f.input :tag_list
  %div.form-group
    = f.button :submit